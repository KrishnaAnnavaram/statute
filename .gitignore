# output/ is committed: it holds the latest pipeline run for each stage
# (regenerated by 01_inventory.py through 08_graph.py).
# See output/*/latest.json for the current run version of each stage.

# Third-party reference harness (COBOL pipeline, used for design guidance only)
reference/

# Python
__pycache__/
*.pyc
*.pyo

# OS/editor noise
.DS_Store
Thumbs.db
